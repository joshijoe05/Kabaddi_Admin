import 'package:flutter/material.dart';

class Refree extends StatelessWidget {
  const Refree({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('You Are Loged in As A Referee'),
    );
  }
}
